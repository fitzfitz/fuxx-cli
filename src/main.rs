fn main() {
    // Milestone 0: prove the edit -> compile -> run loop works.
    // Real behaviour (wrap an agent, watch its output, notify) comes in later milestones.
    println!("fuxx-cli v{} — toolchain works.", env!("CARGO_PKG_VERSION"));
}
